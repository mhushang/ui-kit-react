export enum SORTER_TYPE {
  asc = 'asc',
  desc = 'desc',
}

export enum COLUMN_ALIGN_TYPE {
  start = 'start',
  center = 'center',
  end = 'end',
}

export enum TABLE_TYPE {
  regular = 'regular',
  minimal = 'minimal',
}
